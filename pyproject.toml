[project]
name = "google-flights"
version = "0.2.0"
description = "CLI tool to search Google Flights and output results as JSON"
requires-python = ">=3.10"
dependencies = [
    "swoop-flights>=0.6.0",
    "click>=8.0",
]

[project.scripts]
google-flights = "search_flights:main"
